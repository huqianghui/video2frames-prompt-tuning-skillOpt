# Copy to .env and fill in real values. Never commit .env.

# Azure Blob Storage holding the pre-extracted video frames.
blob4videodatasets_connection_string=
blob4videodatasets_container_name=process-videos
blob4videodatasets_videos_folder_name=training/videos
blob4videodatasets_frames_folder_name=training/frame

# Azure OpenAI resource credentials (shared by the target model, the LLM
# judge, and SkillOpt's optimizer model).
#
# NOTE: .env holds ONLY credentials and connection strings. Which model each
# role uses (target / optimizer / judge) is configured in
# configs/video2frames/default.yaml — see "Model configuration" in README.md.
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_KEY=
AZURE_OPENAI_API_VERSION=2024-10-21

# Optional: send frames as base64 payloads instead of SAS URLs.
# FRAMES_AS_BASE64=1
